/**
 * Feature Flags - Centralized control for experimental or togglable features.
 */
export const FEATURES = {
  /**
   * URL_SEEDS - Enables board layout synchronization via URL parameters
   * and manual seed loading from the console. 
   * Currently disabled to prevent adverse effects from external seed strings.
   */
  URL_SEEDS: false,
};
