import CombatScreen from "@/app/core/screens/CombatScreen";

/**
 * OmegaView — Gamemaster/Blank Canvas Mode.
 */
const OmegaView: React.FC = () => {
  return <CombatScreen boardType="omega" preconfigStyle="omega" />;
};

export default OmegaView;
